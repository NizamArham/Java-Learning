import java.util.jar.JarFile;

import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class Main {
    public static void main(String[] args) {
        
        //JFrame = a GUI window to add components to 

        /*
        
        JFrame frame = new JFrame(); // creates a frame 

        frame.setTitle("Nizam"); // Title of the frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit out if application
        frame.setResizable(false); // prevent frame from being resize
        frame.setSize(420, 420); // size of the frame
        frame.setVisible(true); //make frame visible

        // ImageIcon image = new ImageIcon("logo.png"); //create an ImageIcon
        // frame.setIconImage(image.getImage()); // change icon of the frame

        frame.getContentPane().setBackground(new Color(255,255,255)); // change the color of background
        

        */

        MyFrame myframe = new MyFrame();
    }

}
