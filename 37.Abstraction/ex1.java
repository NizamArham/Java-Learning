public class ex1 {
    public static void main(String[] args) {
        
        //abstract = abstract classes cannot be instantiated, but they can have a subclass
        //           abstract methods are declared whithout an implementation
        
        //Vehicle vehicle = new Vehicle();

        Car car = new Car();
    
        car.go();
    }
}
